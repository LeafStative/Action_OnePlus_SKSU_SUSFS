name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      repo:
        description: 'Manifest repo'
        required: false
      branch:
        description: 'Manifest repo branch'
        required: true
        default: 'oneplus/sm8475'
      file:
        description: 'Manifest file name'
        required: true
        default: 'oneplus_ace2_v.xml'
      gki_abi:
        description: 'GKI ABI'
        required: false
        type: choice
        options:
          - android12-5.10
          - android13-5.10
          - android13-5.15
          - android14-5.15
          - android14-6.1
          - android15-6.6
      kernel_name:
        description: 'Kernel name'
        required: true
        default: '-android12-9-o-g4beca10de372-hath'
      codename:
        description: 'CPU codename'
        required: true
        default: 'waipio'
      sukisu_enable:
        description: "Enable SukiSU-Ultra"
        required: true
        type: boolean
        default: true
      kpm_enable:
        description: "Enable KernelPatch module"
        required: true
        type: boolean
        default: true
      sukisu_manual_hooks:
        description: 'Use manual hooks to integrate SukiSU-Ultra'
        required: true
        type: boolean
        default: true
      susfs_enabled:
        description: 'Enable susfs'
        required: true
        type: boolean
        default: true

permissions: 
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Install dependencies
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y python3 git curl

      - name: Configure Git
        run: |
          git config --global user.name "LeafStative"
          git config --global user.email "leafstative@gmail.com"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure init arguments
        id: init_args
        run: |
          args=''
          output_string=`echo '_${{ inputs.file }}' | sed -E 's/(_[a-z])?\.xml$//'`

          if [[ '${{ inputs.repo }}' ]]; then
            args+=" -r '${{ inputs.repo }}'"
          fi

          if [[ '${{ inputs.sukisu_enable }}' == true ]]; then
            args+=' -k'
            output_string+='_SKSU'

            if [[ '${{ inputs.kpm_enable }}' == true ]]; then
              args+=' -K'
              output_string+='_KPM'
            fi

            if [[ '${{ inputs.susfs_enabled }}' == true ]]; then
              if [[ ! '${{ inputs.gki_abi }}' ]]; then
                echo 'Error: GKI ABI is required when susfs is enabled.'
                exit 1
              fi

              args+=" -g '${{ inputs.gki_abi }}'"
              output_string+='_SUSFS'
            else
              args+=' -s false'
            fi

            if [[ '${{ inputs.sukisu_manual_hooks }}' == true ]]; then
              args+=' -m'
              output_string+='_MANUAL'
            else
              output_string+='_KPROBES'
            fi
          fi

          echo "args=$args" >> $GITHUB_OUTPUT
          echo "output_string=$output_string" >> $GITHUB_OUTPUT

      - name: Init
        run: |
          ./scripts/init.sh \
            -b '${{ inputs.branch }}' \
            -f '${{ inputs.file }}' \
            -n '${{ inputs.kernel_name }}' \
            -c '${{ inputs.codename }}' ${{ steps.init_args.outputs.args }}

      - name: Download source code
        run: |
          ./scripts/download_src.sh

      - name: Apply patches
        run: |
          ./scripts/apply_patches.sh

      - name: Build kernel
        run: |
          ./scripts/build.sh

      - name: Make AnyKernel3
        run: |
          ./scripts/make_ak3.sh

      - name: Upload AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: 'AnyKernel3${{ steps.init_args.outputs.output_string }}${{ inputs.kernel_name }}.zip'
          path: ./workspace/AnyKernel3/*

      - name: Upload kernel
        uses: actions/upload-artifact@v4
        with:
          name: 'Image${{ steps.init_args.outputs.output_string }}${{ inputs.kernel_name }}'
          path: ./workspace/out/dist/Image
